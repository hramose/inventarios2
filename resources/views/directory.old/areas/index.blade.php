@extends('layouts.app')@section('main-content')    <h1>Areas <a href="{{ url('areas/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Area</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>                <tr>                    <th>@lang('form.sno')</th><th>Area</th><th>Actions</th>                </tr>            </thead>            <tbody>            <?php $x=0;?>            @foreach($areas as $item)                <?php $x++;?>                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('areas', $item->id) }}">{{ $item->area }}</a></td>                    <td>                        <a href="{{ url('areas/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['areas', $item->id],                            'style' => 'display:inline'                        ]) !!}                            {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $areas->render() !!} </div>    </div>@endsection
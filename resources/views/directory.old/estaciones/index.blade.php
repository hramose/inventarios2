@extends('layouts.master')@section('content')    <h1>Estaciones <a href="{{ url('estaciones/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Estacione</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>                <tr>                    <th>@lang('form.sno')</th><th>Estacion</th><th>Actions</th>                </tr>            </thead>            <tbody>            <?php $x=0;?>            @foreach($estaciones as $item)                <?php $x++;?>                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('estaciones', $item->id) }}">{{ $item->estacion }}</a></td>                    <td>                        <a href="{{ url('estaciones/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['estaciones', $item->id],                            'style' => 'display:inline'                        ]) !!}                            {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $estaciones->render() !!} </div>    </div>@endsection